const PI = 3.14;


function sumar(x, y) {
const resultado = x + y; 
return resultado;
}
function restar(x, y) {
const resultado = x - y;
return resultado
}
function dividir(x, y) {
const resultado = x / y;
return resultado;
}
const multiplicar = (a, b) => { 
const resultado = a * b;
return resultado;
};

export {PI, restar ,sumar,dividir,multiplicar};